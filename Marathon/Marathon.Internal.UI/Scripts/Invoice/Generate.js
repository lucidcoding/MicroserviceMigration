Marathon.Invoice.Generate = function () {

    var initialize = function () {

    };

    return { initialize: initialize };
} ();


$(document).ready(function () {
    Marathon.Invoice.Generate.initialize();
});